{
  "name": "vampires",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "npm run lint --prefix server",
    "lint:fix": "npm run lint -- --fix",
    "format:fix": "npm run format --prefix server",
    "typecheck": "npm run typecheck --prefix server",
    "test": "npm run test --prefix server",
    "test:all": "npm run test --prefix server",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
