{
  "name": "vampires",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "npm run lint --prefix client-legacy",
    "typecheck": "npm run typecheck --prefix client-legacy",
    "test": "npm run test --prefix client-legacy",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
